@model RecOutletWarehouse.Models.SALES_REP

<h2>@Html.DisplayFor(model => model.SalesRepFirstName) @Html.DisplayFor(model => model.SalesRepLastName) - Details</h2>

<fieldset>
    <legend>Sales Representative Invormation</legend>

    <div class="detail-field-header">
         First Name
    </div>
    <div class="detail-field-value">
        @Html.DisplayFor(model => model.SalesRepFirstName)
    </div>

    <div class="detail-field-header">
         Last Name
    </div>
    <div class="detail-field-value">
        @Html.DisplayFor(model => model.SalesRepLastName)
    </div>

    <div class="detail-field-header">
         Phone Number
    </div>
    <div class="detail-field-value">
        @Html.DisplayFor(model => model.SalesRepPhone)
    </div>

    <div class="detail-field-header">
         E-Mail Address
    </div>
    @if (Model.SalesRepEmail == null) {
        <div class="notspecified detail-field-value">There is no e-mail address on file for this rep.</div>
    }
    else{
        <div class="detail-field-value">
            @Html.DisplayFor(model => model.SalesRepEmail)
        </div>
    }
    <div class="detail-field-header">
         Associated Product Lines
    </div>
    @if (Model.PRODUCT_LINE.Count == 0) {
        <div class="notspecified detail-field-value">This rep is not currently associated with any product lines.</div>
    }
    else{
    <div class="detail-field-value">
        This is the sales rep for the following product lines:
            <ul class="no-bullet no-top-bottom-margin">
            @foreach (var item in Model.PRODUCT_LINE) {
                <li class="vendorproductline">@Html.DisplayFor(modelItem => item.ProductLineName) (Vendor: @Html.DisplayFor(modelItem => item.VENDOR.VendorName))</li>
            }
            </ul>
        </div>
    }
</fieldset>
<p>
    @Html.ActionLink("Edit", "EditRep", new {  id=Model.RepID })
</p>
